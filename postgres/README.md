# PostgreSQL Load Generator

This project uses Docker to spin up a PostgreSQL instance and a Python-based load generator that inserts rows every few seconds.

## 🚀 Usage

```bash
docker-compose up --build
